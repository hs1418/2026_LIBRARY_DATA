# 우리 전래동화 AI 도서관 — System Architecture

> 100년 전 딱지본 옛이야기를 아이가 이어 쓰는 창작 그림책 생성 도구
> 2026 도서관 데이터 활용 공모전 — 융합콘텐츠 제작 부문

> 설계 결정의 근거는 `docs/adr/`에 별도 기록. 이 문서는 전체 조망용이며, 개별 결정의 기각 대안·트레이드오프는 해당 ADR을 참조한다.
> 컨셉·일정·역할은 `docs/PLAN.md`, 공모전 규칙·심사기준은 `CLAUDE.md`.

---

## 1. FR/NFR 정의 및 규모 추정

### 1-1. Functional Requirements (FR)

| ID | 기능 | 설명 | 화면 |
|----|------|------|------|
| FR-1 | 언어 선택 | 한국어 / English 중 선택. 이후 생성물의 주 언어를 결정 | 1 |
| FR-2 | 옛이야기 선택 | 딱지본 원작 서가에서 이야기 선택. MVP는 1~2편 활성 | 2 |
| FR-3 | 원작 앞부분 열람 | 딱지본 원문을 가공한 앞부분 요약 제시 | 3 |
| FR-4 | 아이 구술 입력 | 마이크로 결말을 말하면 텍스트로 기록. 진행자가 수정 가능 | 4 |
| FR-5 | AI 뒷이야기 생성 | 구술을 아이 말투를 살린 동화체로 정리. **페이지 단위로 분할**하고 한/영 병기 + 키워드 추출 | 로딩 |
| FR-6 | 제본용 PDF 생성 | A4 반접기(A5) 규격. 상단 그림 빈칸 + 하단 글, 표지·판권기 포함한 완전본 | 5 |
| FR-7 | 상상 영수증 | 구술에서 뽑은 키워드 + 정보나루 기반 추천도서(청구기호 포함) | 5-모달 |

**FR에서 명시적으로 제외된 것** (목업에 있으나 스코프 밖 — 3-5 참조): 이메일 PDF 전송, 디지털 아카이브·전광판 전송.

### 1-2. Non-Functional Requirements (NFR)

| ID | 항목 | 목표 | 근거 |
|----|------|------|------|
| NFR-1 | 응답 시간 | 정적 조회 API < 200ms | 전부 로컬 DB 읽기라 자연히 충족 |
| NFR-2 | AI 생성 시간 | 뒷이야기 생성 < 15s (타임아웃) | 아이가 결과를 기다리는 실제 인내심 한계 |
| NFR-3 | 가용성 | 워크숍 세션 중 무중단 | 아이 앞에서 실패 = 회복 불가능한 경험 손상 |
| NFR-4 | 동시 사용자 | 1명 | 워크숍은 진행자 1인이 1대로 순차 진행 |
| NFR-5 | 보안 | 시크릿 환경변수, LLM 출력 XSS 방어, HTTPS(배포본) | 아동 대상 서비스이므로 생성물 안전성이 최우선 |
| NFR-6 | 개인정보 | **아동 개인정보 미수집·미저장** | 아이 이름은 PDF 인쇄용으로만 사용, DB 저장 금지 |
| NFR-7 | 배포 | Render 무료 티어 | 제출·시연영상용, 24/7 불필요 (ADR-0003) |

### 1-3. Back-of-Envelope 규모 추정

```
동시 사용자: 1명 (워크숍 현장 1대 + 발표 시연 1대, 동시접속 개념 없음)
데이터량:   Story 1~2행(정적) + Session 하루 수십 건 → DB 총량 < 5MB
호출 빈도:  세션당 외부 API ~3회 (generate LLM 1, 정보나루 1, 추천 LLM 1)
            하루 피크 20세션 → ~60회/일 → 0.001 req/s
AI 비용:    60 req × ~$0.005 = ~$0.3/day (Gemini 기준)
```

**이 숫자가 이 문서 전체의 쳐내기 근거다.** 0.001 req/s 규모에서 캐싱·큐·로드밸런싱·회로차단기는 전부 과설계이며, 6장에서 각각 임계점과 함께 접는다.

---

## 2. 데이터 모델 & DB 선정

### 2-1. DB 선정 근거

PostgreSQL + SQLAlchemy/Alembic. 데이터가 5MB 미만이라 SQLite로도 충분하지만, 개발자가 직전 프로젝트(AI-DO)에서 쓰던 스택이라 **학습 비용 0**이고 Render의 Postgres addon과 그대로 붙는다. 규모가 선정 근거가 못 되는 상황에서는 팀의 숙련도와 배포 경로가 판단 기준이 된다.

### 2-2. ERD

```
┌─────────────────────────────────┐
│ Story  (사전 시딩 · 전 필드 정적)  │
├─────────────────────────────────┤
│ id              PK              │
│ title           콩쥐팥쥐          │
│ emoji           🐸               │  ← 서가 카드용 (디자인 목업)
│ keyword         권선징악          │  ← 서가 카드 라벨
│ intro_summary   원작 앞부분 요약   │
│ intro_image     딱지본 스캔 경로   │
│ bibliography    JSONB 판권기      │  ← 국가서지 API 조회 결과 (시딩 시 1회)
│ fixed_keywords  TEXT[]           │  ← 영수증 폴백용
│ recommend_books JSONB            │  ← 정보나루 실패 시 폴백 (ADR-0002)
└────────────┬────────────────────┘
             │ 1:N
┌────────────▼────────────────────┐
│ Session  (런타임 · 워크숍 1회분)   │
├─────────────────────────────────┤
│ id              PK              │
│ story_id        FK → Story      │
│ lang            'ko' | 'en'     │
│ child_speech    구술 원문         │
│ pages           JSONB           │  ← 페이지 분할 결과 (2-3 참조)
│ keywords        TEXT[]          │  ← generate 응답에서 함께 추출
│ created_at      TIMESTAMP       │
└─────────────────────────────────┘

※ 아이 이름은 어느 테이블에도 없다 — PDF 렌더링 시 요청 파라미터로만 전달 (NFR-6).
```

### 2-3. Session.pages JSONB 구조

디자인 목업의 PDF 미리보기가 페이지마다 다른 문장을 담고 있어, LLM이 뒷이야기를 **페이지 단위로 분할해 반환**해야 한다. 단일 문자열로 받아 서버에서 자르면 문장이 어색하게 끊기므로, 분할 자체를 LLM에 맡긴다.

```json
{
  "pages": [
    { "no": 1, "ko": "두꺼비가 깨진 독을 막아주었어요.", "en": "The toad blocked the broken pot." },
    { "no": 2, "ko": "콩쥐는 물을 가득 채웠어요.",       "en": "Kongjwi filled it with water." },
    { "no": 3, "ko": "그래서 잔치에 갈 수 있었어요.",     "en": "So she could go to the party." }
  ],
  "keywords": ["권선징악", "두꺼비의보은", "창의적해결"]
}
```

- 페이지 수는 3~5로 프롬프트에서 제한 — 아이 집중력과 제본 두께를 함께 고려한 값.
- `keywords`를 같은 응답에 포함시켜 **추가 LLM 호출 없이** 영수증을 채운다 (ADR-0002).

---

## 3. API 설계

### 3-1. 인증

**인증 없음.** 워크숍 현장의 단발 세션이고 로그인·계정·히스토리를 만들지 않기로 했다 (ADR-0001). 개인화는 "세션 내 입력에 따라 결과가 달라진다"는 의미이지 사용자를 식별한다는 뜻이 아니므로 인증이 필요 없다.

### 3-2. 엔드포인트

| 메서드 | 경로 | 용도 | 성격 |
|--------|------|------|------|
| GET | `/api/stories` | 서가 목록 (제목·이모지·키워드) | 정적 |
| GET | `/api/stories/{id}` | 원작 앞부분·스캔 이미지·판권기 | 정적 |
| POST | `/api/stories/{id}/generate` | 구술 → 페이지 분할·한영·키워드 → Session 저장 | **런타임 LLM** |
| GET | `/api/sessions/{id}` | 생성 결과 (미리보기·영수증 데이터) | 읽기 |
| GET | `/api/sessions/{id}/pdf` | A5 완전본 PDF 생성·다운로드 | 렌더링 |
| GET | `/api/sessions/{id}/recommendations` | 키워드 → 정보나루 → LLM 추천 | **런타임 외부** |

**런타임 실패 가능 지점은 굵게 표시한 2개뿐이고, 둘 다 폴백이 있다** (ADR-0004). 나머지는 정적 읽기라 사실상 죽지 않는다.

`POST /generate` 요청/응답:

```
Request:  { "lang": "ko", "child_speech": "두꺼비가 독을 막아줘서..." }
Response: { "session_id": 42, "pages": [...], "keywords": [...] }
```

`GET /pdf` 쿼리: `?author_name=김토스` (선택) — 렌더링에만 쓰고 저장하지 않는다.

### 3-3. LLM 출력 XSS 방어

아이 구술과 LLM 생성문이 그대로 화면·PDF에 들어가므로, 저장 전 `html.escape()` 처리하고 프론트는 `textContent`만 사용한다 (`innerHTML` 금지). PDF는 WeasyPrint가 HTML을 렌더링하므로 **템플릿에 값을 꽂을 때 이스케이프가 빠지면 그대로 실행되는 경로**가 된다 — Jinja2 자동 이스케이프를 켠 상태로 유지하는 것이 이 프로젝트의 유일한 XSS 차단선이다.

### 3-4. 생성 플로우 (골든 패스)

```
[브라우저]                          [FastAPI]                    [외부]
  마이크 → Web Speech API
  (텍스트, 진행자 수정 가능)
       │
       │ POST /generate {lang, child_speech}
       ├──────────────────────────────►│
       │                               │ 프롬프트 조립
       │                               ├──────────────────► Gemini
       │                               │   타임아웃 15s        │
       │                               │   실패 시 1회 재시도   │
       │                               │◄──────────────────┘
       │                               │ pages[] + keywords[] 파싱
       │                               │ html.escape() → Session 저장
       │◄──────────────────────────────┤
       │ {session_id, pages, keywords}
       │
       │ GET /sessions/{id}/pdf
       ├──────────────────────────────►│ Jinja2 + WeasyPrint → A5 PDF
       │◄──────────────────────────────┤ (→ 프린터)
       │
       │ GET /sessions/{id}/recommendations
       ├──────────────────────────────►│
       │                               ├──────────────────► 정보나루 API
       │                               │   타임아웃 8s, 재시도 0
       │                               │   실패 → 즉시 폴백    │
       │                               │◄──────────────────┘
       │                               ├──────────────────► Gemini (추천 문구)
       │◄──────────────────────────────┤
       │ {books[], call_numbers[]}
```

### 3-5. 스코프에서 제외한 목업 기능

| 목업 요소 | 처리 | 이유 |
|---|---|---|
| 이메일 PDF 전송 폼 | **삭제** | SMTP 인프라 신규 도입 + 개인정보 수집. 워크숍은 현장 프린트가 원칙이라 대체 경로가 이미 있음 |
| "도서관 오프라인 전광판으로 전송" 문구 | **삭제** | 디지털 아카이브·전송은 컷된 기능. 구현하지 않는 동작을 UI가 했다고 표시하면 시연 중 허위 진술이 됨 |
| "추천책 위치 뽑기" 버튼 | **유지** | 영수증에 이미 있는 청구기호를 프린트하는 것 — 별도 API 불필요 |
| 아이 이름 입력 | **조건부 유지** | PDF 렌더링 파라미터로만. DB 저장·로그 기록 금지 (NFR-6) |

---

## 4. 고수준 아키텍처 다이어그램

```
        ┌────────────────────────────────────────┐
        │   워크숍 현장 (로컬 실행이 운영 원칙)      │
        │   노트북 1대 (Chrome) + 프린터           │
        └───────────────┬────────────────────────┘
                        │ localhost:8000
                        ▼
  ┌──────────────────────────────────────────────────────┐
  │              FastAPI (Uvicorn)                        │
  │                                                        │
  │   ┌──────────────┐   ┌──────────────┐                │
  │   │ Story Router │   │ Session Router│                │
  │   │  (정적 읽기)  │   │  (생성·PDF)   │                │
  │   └──────┬───────┘   └───────┬──────┘                │
  │          │                   │                        │
  │          │           ┌───────▼────────┐               │
  │          │           │  LLM Client    │───────────────┼──► Gemini API
  │          │           │  15s + 1 retry │               │
  │          │           └───────┬────────┘               │
  │          │                   │                        │
  │          │           ┌───────▼────────┐               │
  │          │           │ 정보나루 Client │───────────────┼──► 정보나루 API
  │          │           │  8s + no retry │               │
  │          │           └───────┬────────┘               │
  │          │                   │ 실패 시                  │
  │          │                   └──► Story.recommend_books │
  │          │                        (고정 폴백)           │
  │          │           ┌────────────────┐               │
  │          │           │  PDF Renderer  │               │
  │          │           │  Jinja2 +      │               │
  │          │           │  WeasyPrint    │               │
  │          │           └───────┬────────┘               │
  │          │                   │                        │
  │          └───────┬───────────┘                        │
  │                  ▼                                     │
  │           PostgreSQL (Story / Session)                │
  │                                                        │
  │   static/fallback_sample.pdf ◄── 오프라인 최종 방어선   │
  │   (id="demo" 요청 시 즉시 서빙)                         │
  └──────────────────────────────────────────────────────┘

  ┌──────────────────────────────────────────────┐
  │  별도 트랙: Render 배포 (ADR-0003)             │
  │  같은 코드베이스 · git push · Postgres addon   │
  │  목적: 원고 첨부 링크 · 시연 영상 녹화 전용      │
  │  워크숍 운영 경로 아님 (24/7 불필요)            │
  └──────────────────────────────────────────────┘

  사전 작업 (런타임 아님):
    딱지본 스캔 ──OCR──► intro_summary 시딩
    국가서지 API ───────► bibliography 시딩
    정보나루 대출데이터 ─► "잊힌 이야기" 발굴 분석
```

**박스가 두 줄기로 완전히 분리된 것이 이 설계의 핵심**이다. 워크숍은 로컬, 배포는 제출용 — 섞으면 도서관 와이파이와 호스팅 서버가 둘 다 살아있어야 하는 구조가 되어 실패 지점이 늘어난다.

### 4-1. 프론트엔드 전략

Vanilla HTML/CSS/JS. 디자이너가 이미 완성된 단일 HTML 목업(화면 5개 + 모달, 고려청자 팔레트 CSS 변수)을 제공했고, 이를 그대로 템플릿으로 흡수하는 것이 가장 빠르다. 빌드 스텝을 추가하는 순간 디자이너가 자기 결과물을 직접 못 고치게 되므로, 협업 구조상으로도 프레임워크 도입이 손해다.

**PDF 템플릿은 같은 CSS 변수(`--primary-celadon` 등)를 공유한다** — 화면과 인쇄물의 톤이 갈리면 완성도 점수(2차 40점)에서 바로 드러난다.

---

## 5. DevSecOps 파이프라인

```yaml
# .github/workflows/ci.yml
on: [push, pull_request]
jobs:
  ci:
    - lint       (ruff)
    - test       (pytest — 골든 패스 스모크 테스트 위주)
    - secret-scan (gitleaks)          # API 키 유출 방지
    - dep-scan    (pip-audit)         # 의존성 취약점
    # deploy는 Render가 git push 감지해 자동 수행 — 별도 잡 불필요
```

**시크릿 관리**: `GEMINI_API_KEY`, `DATA4LIBRARY_KEY`, `NL_API_KEY`, `DATABASE_URL`은 전부 환경변수. `.env.example`이 계약이며 실제 `.env`는 gitignore. Render에는 대시보드 환경변수로 주입한다.

배포 잡을 안 만드는 이유: Render가 GitHub 연동으로 자동 배포하므로 SSH 배포 스크립트가 불필요하다. AI-DO에서 쓰던 EC2+SSH 파이프라인을 그대로 옮기면 이 프로젝트에선 순수한 유지보수 부담만 늘어난다.

---

## 6. 확장성 전략

### 6-1. 현 규모에서 도입하지 않는 것

1-3의 추정(0.001 req/s, 동시 사용자 1명)이 아래를 전부 기각한다. 각 항목의 임계점을 함께 적어, 나중에 판단을 다시 할 조건을 남긴다.

| 항목 | 현 판정 | 재도입 임계점 |
|------|---------|-------------|
| Redis 캐싱 | 불필요 — 하루 60회 호출에 캐시 이득 < 인프라 복잡도 | 정보나루 호출이 시간당 수백 회 |
| 로드밸런싱·멀티인스턴스 | 불필요 — 동시 사용자 1명 | 동시 워크숍 5곳 이상 |
| 비동기 큐(Celery 등) | 불필요 — 생성이 15초 내 끝나고 사용자가 그 앞에 앉아 결과를 기다림 | 생성 시간 60s 초과 또는 배치 처리 요구 |
| 회로차단기 | 불필요 — 고정 타임아웃으로 충분 (ADR-0004) | 다수 도서관 동시 운영 |
| 이야기 등록 CRUD·관리자 페이지 | 불필요 — 1~2편은 시드로 직접 주입 | 도서관이 직접 이야기를 추가하는 운영 모델로 확장 시 |

### 6-2. 대신 확보하는 것 — 3계층 폴백 (ADR-0004)

확장성 대신 **안 죽는 것**에 자원을 쓴다. 규모가 아니라 1회성 워크숍의 성패가 이 프로젝트의 리스크이기 때문이다.

```
1계층  LLM 실패        → 15s 타임아웃 + 1회 재시도 → "다시 말해볼까?" 재입력 유도
2계층  정보나루 실패    → 8s 타임아웃, 재시도 없이 Story.recommend_books 즉시 폴백
3계층  인터넷 전면 장애 → static/fallback_sample.pdf 서빙 + 운영 매뉴얼로 진행
```

---

## 7. 트레이드오프 분석

### 7-1. 설계의 약점과 대안

| # | 약점 | 영향 | 대안 | 안 쓰는 이유 |
|---|------|------|------|-------------|
| 1 | **Gemini 단일 의존** | LLM 장애 = 골든 패스 중단 | 멀티 프로바이더 폴백 | 4주 스코프 초과. 3계층 폴백으로 완화 |
| 2 | **STT가 Chrome 전용** | 다른 브라우저에서 마이크 무용 | 서버 STT | 시연을 Chrome 1대로 하므로 제약이 무의미. 텍스트 수동 입력이 항상 열려 있음 |
| 3 | **타임아웃 15s/8s가 실측 아닌 추정** | 실제 응답이 더 느리면 잦은 폴백 | 실측 후 조정 | W2~W3에 실측 예정 — 지금은 값을 정하는 것 자체가 목적 |
| 4 | **Render 콜드스타트** | 첫 요청 수십 초 지연 | 유료 티어·상시 핑 | 제출·녹화 시점에만 필요하므로 사전 접속 1회로 해소 |
| 5 | **지원 이야기 1~2편** | "겨우 이것뿐?" 인상 | 더 많은 이야기 | OCR·검수 비용이 편당 실질적. 완성도 우선 (PLAN.md MVP 범위) |
| 6 | **LLM 출력이 아동에게 부적절할 위험** | 아이 앞에서 노출 = 회복 불가 | 별도 필터 모델 | 프롬프트 가드레일 + 진행자 확인 단계로 대응. 생성 결과가 화면에 먼저 뜨고 진행자가 프린트를 누르는 구조라 사람이 최종 게이트 |

### 7-2. 핵심 리스크와 대응

```
Risk-1: 딱지본 스캔 OCR 품질이 낮아 원문 확보 실패
  → 대응: W1 검증 항목 (임계 경로). 실패 시 이야기 선정을 OCR 잘 되는 자료로 교체
  → 대응: 앞부분 요약은 어차피 사람이 검수하므로 완전 자동화 불필요

Risk-2: LLM이 아이 말투를 못 살리고 뻣뻣한 문장 생성
  → 대응: W3에 개발자→AI담당자 샘플 전달 → 프롬프트 튜닝 사이클 (핸드오프 표)
  → 대응: 페이지 분할 품질도 같은 사이클에서 검수

Risk-3: 워크숍 당일 인터넷 장애
  → 대응: 3계층 폴백 (6-2)
  → 대응: 사전 생성 샘플 PDF 상비 (W5)

Risk-4: 디자인 템플릿(W2 말)이 늦어 PDF 통합 지연
  → 대응: 개발자가 임시 템플릿으로 먼저 파이프라인 완성 → 나중에 교체
  → 대응: 핸드오프 표의 최우선 임계 경로로 관리
```

---

## 부록 A: 디렉토리 구조

```
library/
├── backend/
│   ├── app/
│   │   ├── main.py
│   │   ├── config.py           # 환경변수 로드 (시크릿 하드코딩 금지)
│   │   ├── models.py           # Story, Session
│   │   ├── routers/
│   │   │   ├── stories.py
│   │   │   └── sessions.py
│   │   ├── services/
│   │   │   ├── llm.py          # Gemini 클라이언트 (15s + 1 retry)
│   │   │   ├── data4library.py # 정보나루 (8s + 폴백)
│   │   │   └── pdf.py          # Jinja2 + WeasyPrint
│   │   └── seed.py             # Story 1~2편 시딩
│   ├── templates/
│   │   ├── book.html           # A5 제본용 (디자인 제공)
│   │   └── receipt.html        # 영수증
│   ├── static/
│   │   └── fallback_sample.pdf # 오프라인 최종 방어선
│   └── alembic/
├── frontend/                   # 디자이너 목업 기반 단일 페이지
├── docs/
│   ├── ARCHITECTURE.md         # 이 문서
│   ├── PLAN.md                 # 컨셉·일정·역할
│   ├── Contest_readme.md       # 공모전 실패사례 교훈
│   └── adr/                    # 0001~0004
└── .env.example
```

## 부록 B: 팀 의존성 (핸드오프)

| 시점 | From → To | 산출물 | 이 문서와의 관계 |
|------|-----------|--------|----------------|
| W1 말 | AI담당자 → 개발자 | 이야기 1~2편 OCR 원문 + 서지정보 | Story 시딩 (2-2) |
| W1 말 | 디자인 → 개발자 | 페이지 레이아웃 초안 | book.html 기반 |
| W2 중 | AI담당자 → 개발자 | 구술→동화체 프롬프트 (**페이지 분할 JSON 사양 포함**) | 2-3 스키마 |
| W2 말 | 디자인 → 개발자 | 완성 HTML/CSS 템플릿 | 4-1, 최우선 임계 경로 |
| W3 초 | AI담당자 → 개발자 | 키워드 추출 사양 + 이야기별 폴백 추천도서 | Story.recommend_books |
| W3 중 | 개발자 → AI담당자/디자인 | 생성 샘플·렌더링 PDF | Risk-2 대응 사이클 |

**W2 중 항목이 갱신됐다**: 디자인 목업이 페이지 분할 PDF를 전제하므로, 프롬프트가 단일 문자열이 아닌 `pages[]` JSON을 반환해야 한다 (2-3). AI담당자에게 이 변경을 전달해야 한다.

## 부록 C: ADR 인덱스

| No | 결정 |
|----|------|
| [0001](adr/0001-api-data-model.md) | API 경계·데이터 모델 (2테이블, 확정 API 5개) |
| [0002](adr/0002-personalized-recommendation.md) | 개인화 추천 (키워드→정보나루→LLM, 세션 범위) |
| [0003](adr/0003-deployment-platform.md) | 배포 플랫폼 — Render |
| [0004](adr/0004-external-call-resilience.md) | 외부 호출 장애 대응 (3계층 폴백) |
