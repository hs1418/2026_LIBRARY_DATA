# Python
__pycache__/
*.pyc
.venv/
.pytest_cache/
.ruff_cache/

# 환경변수 — 시크릿은 절대 커밋 금지
.env

# 산출물
backend/poc_output.pdf
backend/static/generated/
*.log

# OS / IDE
.DS_Store
Thumbs.db
.idea/
.vscode/

# 로컬 스모크 DB
*.db
backend/poc_page*.png

# 배포 부트스트랩 DB
backend/deploy*.db

# 테스트/개발 산출물
backend/*.pdf
backend/*.png

# 디자인팀 표지 원본 PNG(장당 ~9MB, 총 ~83MB) — 리사이즈본만 커밋한다.
# 변환 규칙: 폭 1200px + JPEG q88 → backend/static/covers/{slug}.jpg
Gemini_Generated_Image_*.png

# 디자인팀 삽화 원본(스크린샷 캡처본) — 리사이즈본만 커밋한다.
# 변환 규칙: 폭 1000px + JPEG q88 → backend/static/story/{slug}/p{n}.jpg
2026-*.png
